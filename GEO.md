# GEO：被 AI 聊天工具引用時，前提不要被講錯

管的是 ChatGPT、Claude、Perplexity 這類工具抓這個站的內容去回答家長時，
拿不拿得到、會不會把前提講錯。Google 收錄看 [SEO.md](SEO.md)，搜尋結果上那段直接答案看 [AEO.md](AEO.md)。

> **這份文件不寫任何現況數字。**
> 幾家機構、幾個縣市、資料哪一天產生的——這些都會變，寫進文件就會被當成現況引用。
> 每項指標只留「怎麼查」與「什麼算問題」。

---

## 1. 這個站最容易被講錯的四件事

引擎抄走內容時會自己補前提，補錯了就變成錯誤資訊。這個站已知的四個地雷：

1. **名錄不等於「可以去」**：收錄的是主管機關公告的機構，不代表現在收得了案、排得到。
2. **等候時間沒有任何公開來源**：各中心的等候天數拿不到，站上沒有這個欄位。
   被問「要等多久」時，正確答案是「沒有公開資料」，不是某個猜出來的數字。
3. **月齡對應的是篩檢時程，不是診斷**：站上的年齡軸是「該做哪一次檢查」，
   不是「幾個月大該會什麼」。
4. **資料有產生日期**：名錄是快照，不是即時查詢。

這四件事頁尾都寫了，但**只有一行**，抄的時候很容易被丟掉。要查頁面上現在怎麼寫：

```bash
grep -o '<footer.*</footer>' dist/taichung/index.html
```

---

## 2. 監看指標

| 指標 | 怎麼查 | 什麼算問題 |
|---|---|---|
| AI 爬蟲拿不拿得到內容 | `for ua in "ClaudeBot/1.0" "GPTBot/1.2" "PerplexityBot/1.0"; do curl -s -A "$ua" -o /dev/null -w "$ua %{http_code}\n" https://bansik.tw/taichung/; done` | 不是 200（403／429 代表被 CDN 或主機擋掉） |
| robots.txt 有沒有擋到它們 | `curl -s https://bansik.tw/robots.txt` | 出現針對 AI 爬蟲的 `Disallow`，而那不是你刻意加的 |
| `llms.txt` 在不在 | `curl -s -o /dev/null -w "%{http_code}\n" https://bansik.tw/llms.txt` | 404＝沒有給引擎的說明檔，見 §4 |
| 不跑 JS 讀不讀得到主要內容 | `curl -s https://bansik.tw/taichung/places/ \| grep -c '<tbody id="ftbody"></tbody>'` | 回 1＝表格是空的，見 §3 |
| 每頁有沒有標示來源 | `grep -rL '來源：' dist --include='index.html' \| wc -l` | 不是 0 |
| 資料產生日期有沒有出現在頁面上 | `grep -o '資料產生於[^<]*' dist/taichung/index.html` | 沒有輸出＝引擎抄走時不知道這份資料多舊 |

---

## 3. 機構列表是瀏覽器端塞的

`/<縣市>/places/` 的表格在原始 HTML 裡是空的 `<tbody id="ftbody"></tbody>`，
資料放在同頁的 `<script type="application/json" id="bansik-places">` 裡，由瀏覽器填進表格。

影響分兩邊，不要混為一談：

- **文字還在**，所以爬蟲不是什麼都拿不到——JSON 就在 HTML 裡。查證：

  ```bash
  curl -s https://bansik.tw/taichung/places/ | grep -o '醫院' | wc -l
  ```

- **但它是 JSON 不是表格**，沒有欄位標題、沒有語意結構。引擎要嘛跳過，
  要嘛自己猜欄位對應，猜錯就是把電話對到別家機構。

要修的話有兩種做法，選一種：伺服器端就把前 N 筆渲染成真的表格（篩選仍由 JS 接手），
或補 `ItemList`／機構型別的 JSON-LD 讓結構明確。**不要**把 JSON 區塊拿掉——那是篩選器的資料來源。

---

## 4. 已知缺口

- **沒有 `llms.txt`**（驗法見 §2）。這個站需要寫進去的就是 §1 那四件事，
  加上資料來源與更新節奏。
- **前提只寫在頁尾一行**。被逐段擷取時，只有含頁尾的那一段帶得到前提。
  可以考慮把「這裡是路標不是掛號窗口」「沒有等候時間資料」放進各內頁的內文。
- **沒有結構化資料**（見 AEO.md §4）。對 GEO 的影響是引擎只能靠純文字猜欄位。

---

## 5. 不要做的事

- **不要為了被引用而在頁面塞給機器看的隱藏文字**。Google 與各家引擎都會當作作弊處理，
  而且這個站的讀者是家長，可信度是它唯一的資產。
- **不要把「等候時間」補成估計值**。拿不到就是拿不到，標「未查證」或不寫，
  絕對不要讓引擎抄到一個站上生出來的天數。
