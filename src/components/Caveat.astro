---
// 引用前提。頁尾其實也寫了，但只有一行、又在最底下——
// 搜尋引擎與 AI 工具是逐段抄的，抄到機構表格那一段時不會帶到頁尾，
// 於是「名錄不等於現在收得了案」「等候時間沒有公開資料」這兩件事就掉了。
// 所以在會被單獨抄走的內頁，把前提放進內文裡。
//
// kind 決定講哪一句：
//   list  機構名錄（places、map）
//   wait  流程與時程（steps、checkups）
//   money 補助（subsidy）

interface Props {
  kind: 'list' | 'wait' | 'money';
}
const { kind } = Astro.props;

const TEXT = {
  list: '這份名單是主管機關公告的機構，不代表它現在收得了案或排得到。'
      + '各中心要等多久沒有任何公開資料，這個站也就沒有這個欄位——'
      + '要知道現況只能直接打電話問。',
  wait: '這條時程講的是「該做哪一次檢查」，不是「幾個月大該會什麼」。'
      + '每個孩子的發展速度本來就不同，篩檢沒過也不等於診斷。',
  money: '補助的金額與資格以主管機關最新公告為準，這裡不代為試算，'
       + '也不保證申請一定通過。',
};
---

<p class="caveat">{TEXT[kind]}</p>
